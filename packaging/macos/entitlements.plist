<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
      Hardened runtime entitlements for the signed app. Empty by default —
      a plain Rust + wgpu app doesn't need any. If notarization or runtime
      misbehaves, common entitlements to add:

        <key>com.apple.security.cs.allow-jit</key>
        <true/>

        <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
        <true/>

        <key>com.apple.security.cs.disable-library-validation</key>
        <true/>

      See: https://developer.apple.com/documentation/security/hardened_runtime
    -->
</dict>
</plist>
